<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Student</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h2>Edit Student Details</h2>
    <form action="/update/<%= student._id %>" method="POST" class="form-box">
      <div class="form-group">
        <input name="name" type="text" value="<%= student.name %>" placeholder="Full Name" required>
      </div>
      <div class="form-group">
        <input name="email" type="email" value="<%= student.email %>" placeholder="Email Address" required>
      </div>
      <div class="form-group">
        <input name="branch" type="text" value="<%= student.branch %>" placeholder="Branch (e.g. CSE)" required>
      </div>
      <div class="form-group">
        <input name="year" type="number" value="<%= student.year %>" placeholder="Year (1-4)" min="1" max="4" required>
      </div>
      <button type="submit">Update Student</button>
    </form>
    <a href="/" class="back-link">← Back to List</a>
  </div>
</body>
</html>
